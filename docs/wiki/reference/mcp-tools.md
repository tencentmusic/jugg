# MCP Tools

This reference is the home for exact Jugg MCP tool names, input schemas, output fields, and public availability. Use the capabilities pages to choose the workflow, then use this page when you need the concrete tool contract.

Authoritative runtime source: MCP `tools/list`.
