interface:
  display_name: "Issue Handler"
  short_description: "Investigate Jugg bugs and assess feature requests"
  default_prompt: "Use $issue-handler to classify and handle the supplied Jugg issue from the available evidence."
