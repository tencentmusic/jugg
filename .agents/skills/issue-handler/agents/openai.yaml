interface:
  display_name: "Issue Handler"
  short_description: "Investigate, fix, and validate Jugg issues"
  default_prompt: "Use $issue-handler to investigate the supplied Jugg issue and determine its actual cause from the available evidence."
