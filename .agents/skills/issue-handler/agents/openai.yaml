interface:
  display_name: "Issue Handler"
  short_description: "Investigate, fix, and validate Jugg issues"
  default_prompt: "Use $issue-handler to investigate the supplied Jugg issue, fetch referenced reports, complete requested repository work, verify it, and return the structured decision."
