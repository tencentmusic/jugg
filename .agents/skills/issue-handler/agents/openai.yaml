interface:
  display_name: "GitHub Issue Handler"
  short_description: "Investigate and handle Jugg GitHub Issues"
  default_prompt: "Use $issue-handler to classify and handle the supplied Jugg GitHub Issue from the available evidence."
