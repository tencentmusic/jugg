# The APK owner :app is not part of a native library invocation, so its Android Gradle Plugin must
# stay unconfigured while the collector still needs its strip configuration.
org.gradle.configureondemand=true
